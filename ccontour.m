% ccontour.m Help file for ccontour MEX-file.
%  ccontour.c
% 
%  Calculates the consensus contours for signal `signal` (vector)
%  with sampling rate `fs` (scalar). Parameters for contour calculation
%  are not currently exposed.
% 
%  The calling syntax is:
% 
% 		consensus_contours = ccontour(signal, fs)
% 

%  This is a MEX-file for MATLAB.
% 
